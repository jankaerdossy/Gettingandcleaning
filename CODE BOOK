train_x = x_train.txt
train_y = y_train.txt
train_subject = subject_train.txt
test_x = x_test.txt
test_y = y_test.txt
test_subject = subject_test.txt
merge_x = the merged datas
merge_y = the merged labels
merge_subject = the merged subjects
features = features.txt
measures = the two columns containing the mean an Std from features
activity = activity_labels.txt
final_data = the final version of the pulled data
subject_len = the number of columns in the merge_subject table
act_len = the number of rows in the activity table
column_len = the number of columns in the final_data table
result = the table containing the calculated means
row = counter in the "for" function
